; 国内网络加速：npm 包走腾讯镜像。
; 若你访问 registry.npmjs.org 很顺畅，直接删掉整个 .npmrc 即可 ——
; package-lock.json 里没有写死任何镜像地址，换 registry 不需要重新生成锁文件。
registry=https://mirrors.tencent.com/npm/

; Electron 二进制下载镜像（约 100MB，走 GitHub Release 在国内很慢）。
; 腾讯镜像没有 electron 目录，npmmirror 的目录布局与 @electron/get 兼容。
; 若你的网络访问 GitHub Release 顺畅，可以删掉这一行。
electron_mirror=https://npmmirror.com/mirrors/electron/
